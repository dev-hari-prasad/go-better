# Project Skills

Reusable, on-demand workflows for agents working on GoBetter live under this directory.

## Available Skills

- `plan/`: evidence-based planning that explores the implementation and documentation surfaces before editing.

Each skill must have a `SKILL.md` file whose `name` matches its folder name. Keep large templates and reference material in that skill's `references/` directory.
